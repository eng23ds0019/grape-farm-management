rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Restrict read and write access to the authenticated user matching the farmer ID recursively
    match /users/{farmerId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == farmerId;
    }
  }
}
