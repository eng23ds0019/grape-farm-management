rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Restricts file reading and writing strictly to the logged-in owner of the files
    match /users/{farmerId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == farmerId;
    }
  }
}
